## Attach vs Mount

- Attach: Connecting EBS volume to EC2 instance (AWS level)
- Mount: Making the attached volume accessible via a directory (Linux level)

An EBS volume must be in the same Availability Zone as the EC2 instance
to be attached.
